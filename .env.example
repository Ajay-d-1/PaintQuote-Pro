# ─────────────────────────────────────────────
#  PaintQuote Pro — Environment Configuration
#  Copy this file to .env and fill in your values
# ─────────────────────────────────────────────

# ── AI (Groq — free at groq.com) ──────────────
GROQ_API_KEY=your_groq_api_key_here

# ── Email (SMTP) ───────────────────────────────
# For Gmail: enable 2FA, then generate an App Password at
# https://myaccount.google.com/apppasswords
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=ajajayd96@gmail.com
SMTP_PASSWORD=dgns hrea sqko fkmp

# Dad's email address — where approval emails are sent
DAD_EMAIL=ajajayd96@gmail.com

# ── App Config ─────────────────────────────────
# Generate a random secret key:  python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change_me_to_a_random_32_char_hex_string

# Public URL of this app (used in email approval button links)
APP_URL=http://localhost:5000

FLASK_ENV=development
